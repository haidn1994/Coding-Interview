/*
	리스트는 다음과 같은 조건만 만족한다면 세부적인 구현에 관계없이 리스트로 인정한다.
	"리스트 자료구조는 데이터를 나란히 저장한다. 그리고 중복된 데이터의 저장을 막지 않는다."
	즉, 리스트는 수학의 집합과는 다른 특징을 가지고 있다.
   
    리스트라는 자료구조는 구현방법에 따라서 다음과 같이 크게 2가지로 나뉜다.

	* 순차 리스트:	배열을 기반으로 구현된 리스트
	* 연결 리스트:	*메모리의 동적 할당*을 기반으로 구현된 리스트

	이번 시간에 구현해 볼 것은 배열을 기반으로 한 순차 리스트가 되겠다.
 */

#include <stdio.h>
#include "array_list.h"

int main(void)
{

}
